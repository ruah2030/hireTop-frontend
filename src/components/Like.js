import React, { PureComponent } from 'react';
import {
  AlertDialog,
  AlertDialogBody,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogContent,
  AlertDialogOverlay,
  AlertDialogCloseButton,
  Button,
  useDisclosure,
  Box,
} from '@chakra-ui/react';
function Like({ children, onAction}) {
 
  const [isLoading, setIsLoading] = React.useState(false);
  return (
    <>
      
    </>
  );
}

export default Like;
